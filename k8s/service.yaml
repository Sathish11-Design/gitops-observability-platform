apiVersion: v1
kind: Service
metadata:
  name: task-manager-api
  namespace: task-app
  labels:
    app: task-manager-api
spec:
  selector:
    app: task-manager-api
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  type: ClusterIP